% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_export.R
\name{ts_export}
\alias{ts_export}
\title{Export Preprocessing Results to CSV}
\usage{
ts_export(
  result,
  dir = "atspR_output",
  prefix = "atspR",
  export_clean = TRUE,
  export_report = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{result}{The list returned by \code{\link[=ts_preprocess]{ts_preprocess()}}.}

\item{dir}{Character. Directory to save files. Created if it does not exist.
Default \code{"atspR_output"}.}

\item{prefix}{Character. Filename prefix. Default \code{"atspR"}.}

\item{export_clean}{Logical. Also export \code{data_clean}. Default \code{TRUE}.}

\item{export_report}{Logical. Also export \code{missing_report} and
\code{scale_params} as CSV. Default \code{TRUE}.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
Invisibly returns a named character vector of exported file paths.
}
\description{
Saves the scaled train and test sets (and optionally the cleaned data and
summary report) produced by \code{\link[=ts_preprocess]{ts_preprocess()}} to CSV files in a specified
directory.
}
\examples{
\dontrun{
data(airquality)
result <- ts_preprocess(airquality, verbose = FALSE)
ts_export(result, dir = "output", prefix = "airquality")
}

}
