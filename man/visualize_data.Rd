% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize_data.R
\name{visualize_data}
\alias{visualize_data}
\title{Visualise Data After Missing-Value Handling}
\usage{
visualize_data(
  handle_result,
  raw_data = NULL,
  cols_per_page = 4L,
  n_sample = 10L,
  verbose = TRUE,
  time_col = NULL,
  x_col = NULL,
  y_col = NULL
)
}
\arguments{
\item{handle_result}{The list returned by \code{\link[=handle_missing]{handle_missing()}}.}

\item{raw_data}{\code{data.frame}. The original (pre-cleaning) dataset.
Required when \code{handle_result$action == "impute"}.}

\item{cols_per_page}{Integer. Maximum subplots per page. Default \code{4}.}

\item{n_sample}{Integer. Rows to show in before/after console table.
Default \code{10}.}

\item{verbose}{Logical (default \code{TRUE}).}

\item{time_col}{Character. Name of the datetime column to use as x-axis.
If \code{NULL} (default), row index is used instead.}

\item{x_col, y_col}{Ignored. Kept for backward compatibility.}
}
\value{
An invisible list with elements \code{scatter_plots} (a list of ggplot
objects, one per page) and \code{before_after} (data.frame or NULL).
}
\description{
Produces scatter plots of each numeric column vs row index (time order).
At most 4 subplots are shown per page -- if there are more than 4 numeric
columns, multiple plots are printed sequentially.
When imputation was performed, imputed points are highlighted in red.
}
\examples{
data(airquality)
ana   <- missing_analysis(airquality, plot = FALSE, verbose = FALSE)
clean <- handle_missing(ana, verbose = FALSE)
viz   <- visualize_data(clean, raw_data = airquality)

}
