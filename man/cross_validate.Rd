% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validate.R
\name{cross_validate}
\alias{cross_validate}
\title{K-Fold Cross-Validation on the Training Set}
\usage{
cross_validate(
  scale_result = NULL,
  train = NULL,
  target_col,
  model_fn = NULL,
  k = 5L,
  min_train_size = 0.2,
  verbose = TRUE
)
}
\arguments{
\item{scale_result}{The list returned by \code{\link[=scale_data]{scale_data()}}; provides
\code{train_scaled}.  Alternatively, supply a plain \code{data.frame} to \code{train}.}

\item{train}{A \code{data.frame}.  Used only when \code{scale_result} is \code{NULL}.}

\item{target_col}{Character.  Name of the response column.}

\item{model_fn}{Function with signature \verb{function(train_fold, val_fold)}.
Must return a named numeric vector / list that includes at least one
performance metric (e.g. \code{c(RMSE = ..., MAE = ..., R2 = ...)}).
A simple linear-regression default is provided when \code{NULL}.}

\item{k}{Integer.  Number of folds (default 5).}

\item{min_train_size}{Numeric in (0, 1).  Fraction of rows reserved as seed
training data before folding begins.  Default \code{0.2} (20 \%).
This ensures fold 1 always has training data available.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
An invisible list with elements:
\describe{
\item{\code{fold_results}}{A \code{data.frame} with one row per fold.}
\item{\code{summary}}{A \code{data.frame} with mean and SD of each metric.}
\item{\code{k}}{Number of folds requested.}
\item{\code{k_evaluated}}{Number of folds actually evaluated.}
\item{\code{min_train_size}}{Seed fraction used.}
\item{\code{target_col}}{Response column name.}
}
}
\description{
Performs walk-forward (time-ordered) k-fold cross-validation using a
user-supplied model function. Each validation fold is always preceded
only by past data, so no future information leaks into training.
}
\details{
A seed training set of size \code{min_train_size} is reserved from the start of
the data before folding, guaranteeing every fold (including fold 1) has
enough data to train on.
}
\examples{
data(airquality)
clean <- airquality[complete.cases(airquality), ]
sp    <- split_data(clean, verbose = FALSE)
sc    <- scale_data(sp, verbose = FALSE)
cv    <- cross_validate(sc, target_col = "Ozone", k = 5)
cv$summary

}
