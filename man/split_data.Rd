% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_data.R
\name{split_data}
\alias{split_data}
\title{Split Data into Temporal Train and Test Sets}
\usage{
split_data(data, train_ratio = 0.8, verbose = TRUE)
}
\arguments{
\item{data}{A \code{data.frame} (rows assumed ordered by time).}

\item{train_ratio}{Numeric in (0, 1).  Fraction of rows for training.
Default \code{0.8} (80 / 20 split).}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
An invisible list with elements:
\describe{
\item{\code{train}}{Training \code{data.frame}.}
\item{\code{test}}{Test \code{data.frame}.}
\item{\code{train_idx}}{Integer vector of training row indices.}
\item{\code{test_idx}}{Integer vector of test row indices.}
\item{\code{ratio}}{Named numeric \code{c(train = ..., test = ...)}.}
}
}
\description{
Performs a \strong{time-ordered} (non-random) train/test split so that the
training set always precedes the test set chronologically – the correct
approach for time-series and any dataset where row order encodes time.
}
\examples{
data(airquality)
clean <- airquality[complete.cases(airquality), ]
sp    <- split_data(clean, train_ratio = 0.7)
nrow(sp$train); nrow(sp$test)

}
