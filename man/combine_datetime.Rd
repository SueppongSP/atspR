% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_datetime.R
\name{combine_datetime}
\alias{combine_datetime}
\title{Combine Separate Date and Time Columns into One Timestamp}
\usage{
combine_datetime(
  data,
  date_col = "date",
  time_col = "time",
  new_col = "datetime",
  date_format = NULL,
  time_type = c("hour", "hhmm", "string"),
  tz = "UTC",
  drop_cols = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{date_col}{Character. Name of the date column. Supports many formats
automatically:
\itemize{
\item \code{"2024-01-31"} or \code{"2024/01/31"}
\item \code{"31-01-2024"} or \code{"31/01/2024"}
\item \code{"01-31-2024"} or \code{"01/31/2024"}
\item \code{"31.01.2024"} or \code{"2024.01.31"}
\item \code{"31 Jan 2024"} or \code{"January 31, 2024"}
\item \code{"20240131"} (compact)
}}

\item{time_col}{Character. Name of the time/hour column.}

\item{new_col}{Character. Name of the new combined column.
Default \code{"datetime"}.}

\item{date_format}{Character. Override auto-detection with a \code{strptime}
format string. Leave \code{NULL} (default) to auto-detect.}

\item{time_type}{Character. How the time column is stored:
\itemize{
\item \code{"hour"}   - integer 0-23, e.g. \code{8}, \code{14}
\item \code{"hhmm"}   - integer HHMM, e.g. \code{830}, \code{1430}
\item \code{"string"} - character \code{"HH:MM"} or \code{"HH:MM:SS"}
}}

\item{tz}{Character. Timezone. Default \code{"UTC"}.}

\item{drop_cols}{Logical. If \code{TRUE} (default), remove \code{date_col} and
\code{time_col} after combining.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
The original \code{data.frame} with a new \code{POSIXct} column prepended
(and optionally the source columns removed).
}
\description{
Many datasets store date and time in separate columns. This function
merges them into a single \code{POSIXct} column ready for \code{\link[=fill_time_gaps]{fill_time_gaps()}}.
The date format is detected automatically.
}
\examples{
# Hour stored as integer
df <- data.frame(
  date = c("2024-01-01", "2024-01-01", "2024-01-01"),
  hour = c(8L, 9L, 11L),
  temp = c(25.1, 25.4, 26.2)
)
df2 <- combine_datetime(df, date_col = "date", time_col = "hour",
                        time_type = "hour")
df2$datetime

# Time stored as "HH:MM" string
df3 <- data.frame(
  date = c("01/01/2024", "01/01/2024"),
  time = c("08:30", "09:00"),
  val  = c(10, 12)
)
df4 <- combine_datetime(df3, date_col = "date", time_col = "time",
                        time_type = "string")

}
