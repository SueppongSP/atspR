% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_na.R
\name{standardize_na}
\alias{standardize_na}
\title{Standardise Missing Value Indicators to NA}
\usage{
standardize_na(
  data,
  na_strings = character(0),
  na_numbers = NULL,
  cols = NULL,
  trim = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{na_strings}{Character vector. Additional strings to treat as \code{NA}.
These are combined with the built-in defaults. Set to \code{character(0)} to
use only the defaults.}

\item{na_numbers}{Numeric vector. Numeric sentinel values to treat as \code{NA},
e.g. \code{-999}, \code{9999}, \code{0}. Default \code{NULL} (none).}

\item{cols}{Character vector. Column names to process. \code{NULL} (default)
processes all columns.}

\item{trim}{Logical. If \code{TRUE} (default), trim leading/trailing whitespace
from character columns before matching.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
The \code{data.frame} with all matched values replaced by \code{NA}.
}
\description{
Converts custom "missing" representations such as \code{"-"}, \code{"Missing"},
\code{"N/A"}, \code{"none"}, \code{"null"}, \code{""}, or any user-defined string/number
into proper \code{NA} values so that the rest of the pipeline can detect and
handle them correctly.
}
\examples{
df <- data.frame(
  date  = c("2024-01-01", "2024-01-02", "2024-01-03"),
  temp  = c(25.1, -999, 26.2),
  humid = c("80", "-", "Missing"),
  wind  = c("N/A", "12", "")
)

# Use defaults + custom
clean <- standardize_na(df,
  na_strings = c("N/A"),
  na_numbers = c(-999)
)
clean

}
