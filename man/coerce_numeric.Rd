% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coerce_numeric.R
\name{coerce_numeric}
\alias{coerce_numeric}
\title{Auto-Convert Columns to Numeric}
\usage{
coerce_numeric(data, cols = NULL, min_success_rate = 0.8, verbose = TRUE)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{cols}{Character vector. Columns to attempt conversion on.
\code{NULL} (default) tries all character and factor columns.}

\item{min_success_rate}{Numeric in (0, 1]. Minimum fraction of non-NA
values that must parse successfully for the column to be converted.
Default \code{0.8} (80\%).}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
The \code{data.frame} with eligible columns converted to numeric.
}
\description{
Scans all character and factor columns and attempts to convert them to
numeric. A column is converted only when the proportion of values that
can be parsed as numbers meets the \code{min_success_rate} threshold.
Values that cannot be parsed become \code{NA}.
}
\details{
This step should run \strong{after} \code{\link[=standardize_na]{standardize_na()}} so that custom missing
indicators (e.g. \code{"-"}, \code{"Missing"}) are already \code{NA} and do not count
against the success rate.
}
\examples{
df <- data.frame(
  date  = c("2024-01-01", "2024-01-02", "2024-01-03"),
  temp  = c("25.1", "26.3", "NA"),
  humid = c("80", "85", "90"),
  label = c("A", "B", "C"),
  stringsAsFactors = FALSE
)
# temp and humid will be converted; date and label will stay as character
result <- coerce_numeric(df)
str(result)

}
