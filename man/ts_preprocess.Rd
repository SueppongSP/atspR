% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_preprocess.R
\name{ts_preprocess}
\alias{ts_preprocess}
\title{Run the Full Preprocessing Pipeline}
\usage{
ts_preprocess(
  data,
  na_strings = character(0),
  na_numbers = NULL,
  min_success_rate = 0.8,
  train_ratio = 0.8,
  impute_method = "linear",
  scale_method = "minmax",
  target_col = NULL,
  k_folds = 5L,
  model_fn = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} with rows ordered by time.}

\item{na_strings}{Character vector. Extra strings to treat as \code{NA},
e.g. \code{c("-", "Missing", "N/A")}. Built-in defaults already cover
common patterns. Default \code{character(0)}.}

\item{na_numbers}{Numeric vector. Numeric sentinel values to treat as \code{NA},
e.g. \code{c(-999, 9999)}. Default \code{NULL}.}

\item{min_success_rate}{Numeric in (0, 1]. Minimum fraction of non-NA values
that must parse as numbers for a character column to be converted to numeric.
Default \code{0.8}.}

\item{train_ratio}{Numeric in (0, 1). Default \code{0.8}.}

\item{impute_method}{\code{"linear"} (default) or \code{"knn"}.}

\item{scale_method}{\code{"minmax"} (default), \code{"zscore"}, or \code{"robust"}.}

\item{target_col}{Character. Response column for cross-validation.
If \code{NULL} (default), CV is skipped.}

\item{k_folds}{Integer. Number of CV folds. Default \code{5}.}

\item{model_fn}{Optional custom model function for CV.}

\item{verbose}{Logical (default \code{TRUE}).}
}
\value{
An invisible list with elements:
\describe{
\item{\code{data_clean}}{Cleaned data.frame.}
\item{\code{train}}{Unscaled training set.}
\item{\code{test}}{Unscaled test set.}
\item{\code{train_scaled}}{Scaled training set -- ready for modelling.}
\item{\code{test_scaled}}{Scaled test set.}
\item{\code{scale_params}}{Scaling parameters (fitted on train only).}
\item{\code{scale_method}}{Scaling method used.}
\item{\code{missing_report}}{Per-variable NA summary table.}
\item{\code{imputation_report}}{String describing the action taken.}
\item{\code{cv_summary}}{CV summary data.frame, or NULL if skipped.}
\item{\code{cv_folds}}{Per-fold CV results, or NULL if skipped.}
\item{\code{plots}}{Named list: boxplot, scatter.}
\item{\code{before_after}}{Before/after imputation sample, or NULL.}
}
}
\description{
Executes all pipeline steps in order:
\enumerate{
\item \code{\link[=standardize_na]{standardize_na()}}  - convert custom missing indicators to NA
\item \code{\link[=missing_analysis]{missing_analysis()}} - assess missing values & draw boxplots
\item \code{\link[=handle_missing]{handle_missing()}}   - drop rows or impute
\item \code{\link[=visualize_data]{visualize_data()}}   - scatter plots per variable vs index
\item \code{\link[=split_data]{split_data()}}       - temporal train/test split
\item \code{\link[=scale_data]{scale_data()}}       - feature scaling (no leakage)
\item \code{\link[=cross_validate]{cross_validate()}}   - k-fold CV on training set (optional)
}
}
\examples{
data(airquality)

# Basic usage
result <- ts_preprocess(
  data         = airquality,
  train_ratio  = 0.8,
  scale_method = "minmax"
)
head(result$train_scaled)

# With custom NA indicators
result <- ts_preprocess(
  data       = airquality,
  na_strings = c("-", "Missing"),
  na_numbers = c(-999),
  scale_method = "minmax"
)

}
